import React from "react";
import { StyleSheet, Text, TouchableOpacity, View } from "react-native";
import { SIZES } from "../../constants";

const ReusableBtn = () => {
  return (
    <View>
      <Text>Reusable Btn</Text>
    </View>
  );
};

export default ReusableBtn;

const styles = StyleSheet.create({});
